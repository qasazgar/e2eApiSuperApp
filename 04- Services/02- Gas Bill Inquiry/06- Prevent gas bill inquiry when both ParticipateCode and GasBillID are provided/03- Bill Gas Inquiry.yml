info:
  name: 03- Bill Gas Inquiry
  type: http
  seq: 3

http:
  method: POST
  url: "{{Url-Dev}}services/financial/inquiry/bill/gas/"
  headers:
    - name: accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "participate_code": "{{participate_code}}",
        "gas_bill_id": "{{Gas-id}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Status code is 400", () => {
          expect(res.status).to.equal(400);
        });

        test("Response time is less than 3000ms", () => {
          expect(res.responseTime).to.be.below(3000);
        });


        let responseData = res.body;

        if (typeof responseData === "string") {
          try {
            responseData = JSON.parse(responseData);
          } catch (error) {
            responseData = {};
          }
        }

        if (
          responseData.data &&
          Array.isArray(responseData.data.non_field_errors) &&
          responseData.data.non_field_errors.includes(
            "Provide either participate_code or gas_bill_id, not both."
          )
        ) {
          console.log("❌ شناسه قبض گاز و کد اشتراک با هم نمی‌توانند وجود داشته باشند");
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
