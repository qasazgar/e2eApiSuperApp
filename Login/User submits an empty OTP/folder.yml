info:
  name: User submits an empty OTP
  type: folder
  seq: 7

request:
  auth: inherit

docs:
  content: |-
    # **User submits an empty OTP**

    **Given** the user is on the OTP verification step \
    **When** the user submits the OTP without entering a code \
    **Then** the system should display a validation error \
    **And** the user should not be authenticated
  type: text/markdown
